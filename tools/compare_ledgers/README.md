# compare_ledgers

Placeholder for the ledger comparison tool.

Inputs will be JSON token ledgers from NERVA and baseline runs. The first useful
version should compare:

- token latency,
- host sync count,
- device allocation count,
- H2D/D2H bytes,
- KV residency decisions,
- kernel launch sequence,
- graph replay/capture events.
