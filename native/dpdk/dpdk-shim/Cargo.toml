[package]
name = "dpdk-shim"
version = "0.1.0"
edition = "2024"
build = "build.rs"
links = "dpdk-shim"
publish = false

[dependencies]
libc = "0.2.176"

[build-dependencies]
bindgen = "0.72"
cc = "1.2"
pkg-config = "0.3"
